# Excluded Historical Research Routes

## 1. Scope of exclusion

The historical source contains many valid experimental and formal research
directions. This repository selects the shortest auditable dependency chain
from positional carry to the mass-built native operator, its operator-zero
predicate, and its faithful real and analytic coordinates, with radial-chart
representation rigidity as a downstream comparison theorem.

Exclusion from this root means:

```text
valid as a research direction; not a dependency of the audited core.
```

It does not mean that the excluded result is false.

## 2. Routes not imported

| Historical route | Research question | Status here |
|---|---|---|
| completed Green operators | explicit radial-flow and boundary completion | excluded |
| weighted bracket–trace reconstruction | reconstruction from bracket data and trace information | excluded |
| enriched provenance-state channels | transport of states carrying additional provenance data | excluded |
| Bessel estimates and energy ledgers | global energy budgets and trace domains | excluded |
| source-state realizations | simultaneous realization of several readings by one state | excluded |
| precompression lifts | recovery of a state before observation or compression | excluded |
| self-adjoint and Cayley realizations | additional operator-theoretic structure | excluded |
| spectral pencils and boundary relations | alternative spectral organization of boundary data | excluded |
| finite-atlas Pythagorean decompositions | visible versus residual energy under a realization hypothesis | excluded |
| uniform contraction bounds | bounds for already-realized lifted states | excluded |
| least-significant-bit and root-tangent atlases | alternative multibase visual and combinatorial coordinates | excluded |
| empirical scanners and cutoff extrapolation | finite numerical evidence for boundary behavior | excluded |
| prime-distribution reconstruction experiments | separate number-theoretic reconstruction questions | excluded |

## 3. Why these routes are unnecessary for the native construction

The proof of `NCG-OPR-007` needs only:

1. positional mass rigidity;
2. the positional-to-real quadratic-domain crosswalk;
3. the definition of boundary closure;
4. the half-chart identity connecting ambient presentation to the native boundary.

It does not need a second global energy identity, a reconstruction theorem, a
source state, or an operator completion. Adding such a route could establish a
stronger theorem, but it would not repair a gap in the existing chart-representation factorization.

## 4. Curvature has a special status

The radial curvature module is included because it supplies an independent
local sign interpretation of the same exponent. Its null locus is an auxiliary detector locus, not another
operator zero. It is not in the proof dependency of the terminal theorem.

## 5. Numerical evidence

Finite numerical studies may be useful for:

- locating candidate resonance times;
- estimating cutoff decay;
- comparing camera presentations;
- separating visible resultant energy from internal component energy.

They are not part of this release's kernel proof and are not needed to
reproduce its theorems.

## 6. Audit boundary

No excluded route should be reintroduced into the core merely to preserve a
historical import alias. A future extension may import this repository and
develop one of these directions without changing the meaning, statement, or
dependency boundary of the core NCG theorems.
